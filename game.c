#include <stdio.h>
int main()
{
	int i, j, k, x, y;
	char n[10];
	printf("Введите размеры карты (x * y) :\n");
	scanf("%d %d", &x, &y);
	for (i = 0; i < x; i++)
		for(j = 0; j < y; j++);
		for(k = 0; k < 5; k++)
		{
			
		}